# 오차예산 생산 런 — 전 빈 전파 (2026-09-21)

이 문서가 `docs/Augur_오차예산_1단계_재실행_2026-09-20.md` 의 **수치를 대체**한다.
운영 fitset·게이트·분모 규약은 그대로고, **바뀐 것은 표본 설계**다.

재실행판은 knot 마다 **대표 빈 하나**로 표를 만들었다. 그런데 응답은 그 빈의 증폭
|I₀/(I₀−I)| 에 심하게 의존하고(실측 9~350), 같은 knot 안에서도 빈에 따라 24~1366 %
흔들린다. **대표 빈을 2.6분 옮기면 robust SD 가 25 % 달라졌다**(2.234 % vs 2.803 %).
표본이 결론을 정하고 있었다.

`docs/Augur_오차예산_감사_지시서_2026-09-19.md` §4.3 이 선언한 규칙이 정확히 이걸
요구한다 — **모든 항을 최종 산출물 시계열로 전파하고, scale 은 맨 마지막에 한 번**.
그래서 빈마다 하나의 응답을 만들어 진짜 시계열을 구성했다. 빈이 산출물의 표본
단위이므로 가중은 자동으로 스캔당이 되고, knot 균등 가중이 사라진다.

재현:

```
python diagnostics/i0_interp_2026-09/production_budget.py \
    --fitset "C:/Doasis_Work/Output/fit setting/FitSet_ANs[430-462nm_P4]_PNs[444-471nm_P3]_cold[438-476nm_P4]_Std.json"
```

산출물: `diagnostics/i0_interp_2026-09/production_budget.csv` (8505행, 빈별
증폭·응답·Δshift), `production_budget_hourly.csv` (146시간).

---

## 0. 이 예산의 적용 범위 — 반드시 먼저 읽을 것

| | |
|---|---|
| 구간 | **2026-05-18 ~ 05-24** (146시간, ambient 빈 8505) |
| 채널 | hot ANs (ch1) · hot PNs (ch2). **cold 는 미포함** — `[TBD-C]` |
| 관측 병합자료 | 1201시간 (~50일) |

**예산은 캠페인의 약 12 % 구간에서 측정됐다.** §3 의 게이트 비교는 같은 시각으로
짝지어 이 불일치를 처리하지만, 원고에는 측정 구간을 명시해야 한다.

이 7일은 전부 `DataIO.clock_epoch_offset_sec` 보정 창(2026-05-18 ~ 05-29 09:29)
**안에** 있다. 창 밖 구간이 없으므로 시계 보정이 인공물을 남겼는지는 이 자료로
검증할 수 없다.

### ⚠ 일부는 "보간" 이 아니라 **외삽** 이다 (`[TBD-F]` · `[TBD-6b]` 해소)

`tools/extract_cal_knots.py` 로 raw 에서 캠페인 전체 ZA·He knot 시각을 뽑고,
`tools/alpha_context_sidecar.py` 로 알파 행마다 최근접 knot 거리를 재고,
`tools/coverage_table.py` 로 **한 실행에서 하나의 표**로 낸다.

추출기는 두 번 독립 확인됐다: 7일치 진단 캐시와 **157/157 knot, 시각차
0.000000 s**, 그리고 hot ZA 블록 수 **1313** 이 알파 헤더 `ZA_count=1313` 과 일치.
cold 도 **164/164, 0.000000 s**.

| 계기 | 파일 | ZA 블록 | He 블록 | 스캔/블록 | 구간 |
|---|---|---|---|---|---|
| hot | 1314 | **1313** | 444 | 34 | 05-18 ~ 07-11 |
| cold | 744 | **743** | 277 | **29** | 05-18 ~ 06-17 |

⚠ 시각 규약: 이 표들의 `day` 는 **0-based** 다(`day = sec/86400`, `sec=(doy−1)·86400`).
`day 137.11 = 2026-05-18 02:38` 이지 05-17 이 아니다.

### 0-A. 참조 커버리지 — 한 실행, 같은 정의

| 계기 | 창 | n | I0 범위밖 | I0 간격>2h | I0 최악 | R 범위밖 | R 간격>2h | R 최악 |
|---|---|---|---|---|---|---|---|---|
| hot ch1 | 전 캠페인 | 76282 | 0.08 % | 0.07 % | 10.6 h | 4.45 % | 0.53 % | 10.6 h |
| hot ch1 | **예산 창 05-18~24** | 9088 | **0.01 %** | **0.00 %** | 1.3 h | **5.81 %** | **0.00 %** | 1.3 h |
| hot ch2 | 전 캠페인 | 76282 | 0.08 % | 0.07 % | 10.6 h | 4.45 % | 0.53 % | 10.6 h |
| hot ch2 | 예산 창 05-18~24 | 9088 | 0.01 % | 0.00 % | 1.3 h | 5.81 % | 0.00 % | 1.3 h |
| cold | 전 캠페인 | 43259 | 0.13 % | 0.34 % | 11.6 h | 2.89 % | **11.03 %** | 11.8 h |
| cold | **cold 창 06-10~16** | 7069 | 0.42 % | 0.00 % | 1.8 h | **17.32 %** | **18.31 %** | 11.8 h |

`I₀ 최근접 knot: hot 중앙 922 s · p99 1794 s · max 3444 s`

⚠ **옛 본문의 "hot 4.1 % · cold 5.0 %, 최악 10.6 h" 는 인용하지 말 것.**
최악 간격 10.6 h 는 이 표에서 재현되지만, 4.1 %/5.0 % 는 어느 양인지 라벨이
확정되지 않았다(범위밖 4.45 % 와 가깝지만 cold 는 어느 쪽과도 안 맞는다).
위 표가 단일 출처다.

**I₀ 는 "보간" 이 맞다** — 외삽 0.08 %, 간격 결손 0.07 %.
**R 은 예산 창의 5.81 % 가 외삽**이고 전부 첫날(05-18)이다.

### 0-B. 외삽의 원인은 둘이고, **주된 쪽은 물리가 아니다**

| | 처리 파일 | ZA 블록 | 파일목록 밖 | 목록 안 품질기각 |
|---|---|---|---|---|
| hot ch2 | 1264 (05-18~**07-09**) | 1313 | **48** | 13 (1.0 %) |
| cold | 752 (05-17~06-17) | 743 | 0 | **75 (10.1 %)** |

**hot 외삽 3,393행의 내역:**

| 원인 | 행 | 비율 |
|---|---|---|
| 마지막 R knot 이후 — **R 생성이 07-09 에서 멈췄다** | 2865 | **84 %** |
| 첫 R knot 이전 — 품질 기각 9블록(05-18 02:42~11:40) | 528 | 16 % |

즉 **hot 외삽의 84 % 는 커버리지 결함이 아니라 기록 문제**다. R 을 남은 이틀치
파일까지 다시 생성하면 사라진다 — 계기 비용 0, 소프트웨어 재실행뿐이다.

⚠ **그래서 "배치 시작·종료에 He 블록을 넣으면 4.45 % 가 사라진다" 는 처방은
hot 에 대해 틀렸다.** He 블록을 아무리 넣어도 R 생성이 안 돌린 구간은 그대로다.

**cold 는 반대다** — 파일목록 잘림 0, 품질기각 **75블록(10.1 %)**. 기각이 몰린
날은 06-17(20) · 05-27(12) · 06-16(11) · 05-23(9) · 06-05(8) 이고, 이는 앞서
잰 R 간격>2h 상위 날짜와 정확히 겹친다. knot 하나가 빠지면 간격이 2배가 되므로
10.1 % 의 기각이 11 % 의 행(예산 창 18 %)으로 증폭된다.

### 0-C. 품질 관문이 스스로 외삽 구간을 만든다 (§6.5)

R knot 은 ZA/He 페어에서 만들어지고 세 관문을 통과해야 한다
(`reflectance_calc` `min_valid_fraction=0.30` · Leff [0.5×,2×] 밴드 ·
`AlphaExportWorker` `r_cal_valid_min=0.90` · `r_cal_omr_max=1e-5`).

| | 채택률 |
|---|---|
| hot CH2 | **99.0 %** (13/1265 기각) |
| cold | **89.9 %** (75/743 기각) |

**hot 에서는 관문이 거의 공짜고, cold 에서는 커버리지의 10 % 를 먹는다.**
그리고 그 대가가 R(t) 항으로 돌아온다 — cold 의 R 항이 SD 분산의 83 %(§9-C),
5시간이 그 90 %(§9-D).

> 광량이 낮다 → 페어 품질 관문이 10 % 를 기각한다 → 행의 11 %(예산 창 18 %)가
> 2 h 넘는 간격에 놓인다 → R(t) 항이 분산을 지배한다

§6.3 의 저광량 게이트는 이 사슬의 첫 칸을 겨냥하지만 **I₀ knot 을 버리는
쪽**이다. cold 에서 실제로 아픈 곳은 **R 쪽 커버리지**이고, 거기는 이미 관문이
있다 — 없어서가 아니라 **있어서** 생긴 결손이다. §6.3 에 그 구분을 써야 한다.

---

## 1. 수렴과 상자고착

| 채널 | 비수렴(NaN) | 상자고착 빈 | 항별 |
|---|---|---|---|
| ANs (ch1) | 0 | **0 / 8505 (0.0 %)** | 전부 0 |
| PNs (ch2) | 0 | **123 / 8505 (1.4 %)** | base 4 · **i0 121** · rt 4 · ef 3 · tri 68 |

고착은 **PNs 의 I₀ 섭동에만** 몰린다(121/123). ANs 는 한 건도 없다.

§5(경계 고착을 수렴으로 보고하지 말 것)에 따라 본문 값은 고착을 **제외**한다.
다만 제외 자체가 편향을 만든다는 점을 기록해 둔다 — 5개 핏(base + 4섭동) 중
하나라도 상자에 닿으면 그 빈을 버리는데, **섭동이 핏을 상자 끝까지 미는 빈이
곧 응답이 가장 큰 빈**이다. 즉 배제규칙이 예산을 위에서 자른다.

| | ΣANs 3중 | 게이트 비 |
|---|---|---|
| 각주: 고착 포함 (n=8505) | 0.0769 ppb | 0.92배 |
| **본문: 고착 제외 (n=8382)** | **0.0758 ppb** | **0.91배** |

**제외 효과 −1.5 %.** 판정을 바꾸지 않는다.

---

## 2. 성분별 scale — Table 2 의 원천

빈 기저, n=8382, 상자고착 제외. `s = rSD/SD` (가우스 1.000, 라플라스 0.727).

| 계열 | 항 | rSD (ppb) | SD (ppb) | s |
|---|---|---|---|---|
| ANs채널 | i0 | 0.0421 | 0.0608 | 0.692 |
| | rt | 0.0326 | 0.1140 | **0.286** |
| | ef | 0.0084 | 0.0087 | 0.958 |
| | **3중** | **0.0658** | **0.1298** | 0.507 |
| PNs채널 | i0 | 0.0412 | 0.0426 | 0.967 |
| | rt | 0.0123 | 0.0337 | **0.366** |
| | ef | 0.0189 | 0.0193 | 0.979 |
| | **3중** | **0.0462** | **0.0583** | 0.792 |
| ΣANs | i0 | 0.0576 | 0.0789 | 0.731 |
| | rt | 0.0253 | 0.0927 | **0.273** |
| | ef | 0.0199 | 0.0206 | 0.969 |
| | **3중** | **0.0758** | **0.1184** | 0.640 |

### 2-A. 지배항은 척도에 따라 뒤집힌다

ΣANs 분산 기여:

| 항 | rSD 기준 | SD 기준 | s |
|---|---|---|---|
| I₀ | **76 %** | 41 % | 0.73 |
| R(t) | 15 % | **56 %** | **0.27** |
| ef | 9 % | 3 % | 0.97 |

R(t) 는 **전형적인 스캔에서는 작고 전체로는 가장 큰** 항이다. "I₀ 가 지배항" 은
rSD 만 본 결론이었다.

---

## 3. §4.3 — 분산은 더해진다, robust scale 은 안 더해진다

| 계열 | 3중 rSD | 제곱합 rSD | 비 | 3중 SD | 제곱합 SD | **비** |
|---|---|---|---|---|---|---|
| ANs채널 | 0.0658 | 0.0538 | 1.222 | 0.1298 | 0.1295 | **1.003** |
| PNs채널 | 0.0462 | 0.0470 | 0.983 | 0.0583 | 0.0577 | **1.012** |
| ΣANs | 0.0758 | 0.0660 | 1.147 | 0.1184 | 0.1235 | **0.959** |

SD 공간에서 **0.3 % · 1.2 % · 4.1 %** 안에서 맞는다. knot 기저(0.946)와 빈
기저에서 재현되므로 관측이 아니라 **법칙**으로 쓴다.

### 3-A. ⚠ SD 공간 재현의 **검증력은 계열마다 다르다**

한 항이 분산을 지배하면 3중 ≈ 그 항이 되고 제곱합도 같은 이유로 그 항에
지배되므로, **제곱합이 맞는 것이 거의 자명**해진다. SD 공간 분산 기여:

| 계열 | i0 | rt | ef | SD 공간 비 | 검증력 |
|---|---|---|---|---|---|
| **PNs채널** | **55 %** | **34 %** | **11 %** | **1.012** | **강함** |
| ΣANs | 41 % | 56 % | 3 % | 0.959 | 중간 |
| ANs채널 | 22 % | 78 % | 0.5 % | 1.003 | 약함 |
| cold | 17 % | 83 % | 0.03 % | 1.022 | 약함 |

**세 항이 비슷한 크기로 섞인 계열은 hot PNs 하나뿐**이고, 거기서 1.012 다.
"네 번 재현" 으로 쓰면 심사자가 이 표를 만들어 온다. 정직한 문안:

> SD 공간 재현은 네 계열 모두에서 성립하지만(0.959–1.022), 세 항이 비슷한
> 크기로 기여하는 계열은 hot PNs 하나이며 거기서 1.012 다. 나머지는 한 항이
> 지배해 검증력이 약하다. 반면 **robust 공간의 이탈(0.983–1.306)은 지배
> 구조와 무관하게 발생**한다.

즉 **SD 공간 주장은 절제해서, robust 공간 기각은 강하게** 쓴다. cold(1.306)는
후자의 최강 사례로 남는다.

**robust 공간의 오차는 보정할 수 없다.** 1.222 / 0.983 / 1.147 — 부호도 크기도
예측되지 않고, 예측하려면 joint 를 이미 돌렸어야 한다. 제곱합은 **보정 불가,
대체만 가능**하다.

메커니즘은 성분별 `s` 열에 있다. ef 는 거의 가우스(0.96~0.98), i0 는 중간
(0.69~0.97), rt 는 극단(0.27~0.37). **모양이 다른 것들을 robust scale 로 더하면
안 되는 이유**가 이 열이다. 분산은 모양에 무관하므로 더해진다.

---

## 4. Δshift 분해 — 예산은 분지 전환이 아니다

I₀ knot 하나를 빼면 핏이 다른 국소최소로 넘어갈 수 있다(max |Δshift| 4.21 px).
그러면 예산이 "보간오차의 전파"(§4)가 아니라 "분지 불안정"(§5)을 재고 있을 수
있다. 갈라서 확인했다.

| 구간 | n | 비율 | rSD | SD | 분산기여 |
|---|---|---|---|---|---|
| < 0.1 px | 1259 | 15.0 % | **0.1184** | 0.1584 | 28.5 % |
| 0.1–1 px | 6727 | 80.3 % | 0.0719 | 0.1101 | 69.2 % |
| ≥ 1 px | 396 | **4.7 %** | **0.0625** | 0.0780 | **2.3 %** |

`p50 0.232 · p90 0.686 · p99 1.643 · max 4.213 px`

분지 전환 의심 구간(≥1 px)은 빈의 4.7 %, **분산의 2.3 %**이고 rSD 가 **가장 작다.**
통째로 빼도 예산은 −0.2 % 움직인다.

**의심 빈을 더 조이면 예산이 커진다:**

| 잘라내기 | n | ΣANs 3중 | 변화 |
|---|---|---|---|
| 없음 | 8382 | 0.0758 | — |
| < 1.0 px 만 | 7986 | 0.0756 | −0.2 % |
| < 0.5 px 만 | 6977 | 0.0770 | +1.6 % |
| < 0.2 px 만 | 3539 | 0.0885 | **+16.8 %** |

§4 와 §5 는 서로 오염시키지 않는다.

**왜 Δshift 가 작은 층의 응답이 큰가** — 증폭이 답한다:

| 층 | 증폭 중앙 (ANs) | 증폭 중앙 (PNs) | \|3중\| 중앙 |
|---|---|---|---|
| < 0.1 px | **26.0** | **21.8** | **0.0787** |
| 0.1–1 px | 55.9 | 46.0 | 0.0490 |
| ≥ 1 px | 141.4 | 90.6 | 0.0479 |

증폭이 낮은 = 흡수가 깊은 = 신호가 센 스캔이 Δshift 는 안 움직이면서 ppb 응답은
크다. §6 의 대리변수 음의 상관과 같은 구조다.

### 항별로 누가 shift 를 미는가

| 항 | p50 | p90 | max | ≥1 px |
|---|---|---|---|---|
| i0 | 0.179 | 0.743 | 4.755 | 5.9 % |
| **rt** | **0.006** | 0.020 | **0.097** | **0.0 %** |
| ef | 0.151 | 0.343 | 2.675 | 0.1 % |
| tri | 0.232 | 0.686 | 4.213 | 4.7 % |

**R(t) 섭동은 shift 를 사실상 안 움직인다** — 순수 진폭 섭동이다. shift 이동은
전적으로 I₀ 와 etalon f 쪽이다. §4 에서 세 항을 한 문장으로 묶으면 안 된다:
**모양을 흔드는 것(I₀, f)과 크기만 흔드는 것(R)** 으로 나눠야 한다.

---

## 5. §4.5 — 게이트 비교

게이트 연산자는 **1시간 평균의 연속차**: `1.4826·median|Δ_1h| / √2`.

| | 시간 | 연속쌍 | 게이트 |
|---|---|---|---|
| 예산 시간계열 | 146 | 145 | 0.0575 ppb |
| 관측 **전체** | 1201 | 1197 | 0.0832 ppb |
| 관측 **(예산과 짝)** | 107 | 106 | **0.0727 ppb** |

| 비교 | 비 | 지위 |
|---|---|---|
| 빈 해상도 vs 캠페인 게이트 | 0.91배 | 병기 (연산자 불일치) |
| 시간평균 vs 캠페인 게이트 | 0.69배 | 병기 (표본 불일치) |
| **시간평균 vs 짝지은 관측** | **0.79배** | **본값** |

짝지은 구간이 캠페인 전체보다 조용하다(0.0727 < 0.0832) — 즉 캠페인 전체를
분모로 쓰면 **예산에 유리한 쪽으로 틀린다.** 0.79 가 like-for-like 다.

짝이 146 → 107시간으로 주는 것은 병합자료에 `ans_ppb` 가 8개 미만인 시간이 39개
있기 때문이다.

**너무 낮지도 않다.** 0.79 는 관측 변동의 대부분을 예산이 설명한다는 뜻이다.
0.2 였다면 예산이 기술하지 못하는 무언가가 지배한다는 뜻이었을 것이다.

---

## 6. 모양 통계가 처방을 고른다 (§6.1a)

`s` 가 작다는 건 **꼬리가 무겁다**는 뜻이지 **시간적으로 뭉쳐 있다**는 뜻이
아니다. 무작위 빈에 흩어진 두꺼운 꼬리라면 탐지·플래그 처방이 안 먹는다.
집중도와 시간뭉침을 갈라 쟀다(146시간, 8382빈):

| 항 | s | 분산 50 % | 분산 90 % | 연속구간 수 (무작위 기대) |
|---|---|---|---|---|
| i0 | 0.731 | 15시간 (10.3 %) | 60시간 (41.1 %) | 23 (36) |
| **rt** | **0.273** | **4시간 (2.7 %)** | **21시간 (14.4 %)** | **8 (18)** |
| ef | 0.969 | 37시간 (25.3 %) | 112시간 (76.7 %) | 23 (27) |
| tri | 0.640 | 10시간 (6.8 %) | 52시간 (35.6 %) | 18 (34) |

**s 가 집중도를 예측한다.** s 순서(rt < tri < i0 < ef)가 집중도 순서와 일치한다.
시간뭉침도 확인된다 — rt 상위 시간은 연속구간 8개, 무작위 기대 18개. ef 는
23 vs 27 로 **무작위와 구분되지 않는다.**

rt 최악 구간: **2026-05-20 UTC 13–14시 두 시간이 분산의 39.8 %**를 진다.

| s | 뜻 | 처방 |
|---|---|---|
| ≈ 1 (ef) | 모든 스캔에 고르게 퍼짐 | 모델을 고쳐야 함 |
| 0.7–1 (I₀) | 참조 측정 잡음 | 평균 스캔 수 |
| ≪ 0.7 (R) | 소수 구간에 집중 | 탐지·플래그 |

### 6-A. 처방 효과는 **달성 가능한 상한**이다

| | ΣANs 구조항 (SD) | 배수 |
|---|---|---|
| 운용 | 0.1235 ppb | 1.00 |
| 블록 ×4 만 | 0.1028 | 0.83 |
| R 구간 플래그 만 | 0.0854 | 0.69 |
| 둘 다 | 0.0512 | 0.41 |

⚠ **이 둘은 가정이지 예측이 아니다.**

- 블록 ×4 → i0 SD 를 정확히 1/2 로 (N^−0.5). **§10-B 가 이 가정을 검증하지
  못한다고 결론냈다** — 부분블록 분할로는 `n < N` 만 볼 수 있고, 잴 수 있는
  구간의 지수가 채널마다 **0.104 (ANs) 대 0.416 (PNs)** 로 갈린다. ANs 는
  가정과 **어긋나는 방향**이다. 배수는 **미검증**이다.
- R 플래그 → rt 의 SD(0.0927)를 rSD(0.0253)로 치환. **꼬리만 정확히 도려내는
  완벽한 플래거**를 가정한다. 재현율·오경보율은 측정된 바 없다.

0.41배는 하한 추정치가 아니라 **최선의 경우**다.

**순서 역전 자체는 확고하다** — rt 는 평균으로 안 줄고 i0 는 주니, "플래그 먼저"
는 어떤 플래거 성능을 가정하든 유지된다.

---

## 7. 대리변수는 폐기한다

### 7-A. 절대 응답 ~ 증폭

| 채널 | 항 | Pearson | R² | Spearman | log기울기 | logR² |
|---|---|---|---|---|---|---|
| ANs | i0 | −0.095 | 0.009 | −0.201 | −0.244 | 0.027 |
| ANs | rt | −0.242 | 0.059 | −0.492 | −0.874 | 0.235 |
| ANs | **tri** | −0.218 | 0.048 | −0.340 | −0.477 | **0.092** |
| PNs | i0 | −0.022 | 0.000 | −0.004 | +0.021 | 0.000 |
| PNs | rt | −0.279 | 0.078 | −0.447 | −1.069 | 0.187 |
| PNs | **tri** | −0.084 | 0.007 | −0.034 | −0.095 | **0.003** |

Spearman 이 Pearson 보다 강해지지만 **전부 음수**다. 핵심 항(tri)의 logR² 이
0.092 / 0.003 — 예측력 없다. 꼬리가 무거운 두 양을 선형 Pearson 으로 쟀다는
의심은 정당했으나, 순위·로그 공간에서도 살아나지 않는다.

### 7-B. 상대 응답 (|응답| / 검색값)

"절대로는 못 맞혀도 **상대로는 고정값**일 수 있다"를 확인했다. 판정 기준은
중앙값이 아니라 흩어짐 — robust 변동계수 `CRV = rSD/|중앙|` 이 절대공간보다
작아져야 상대화가 뭔가를 정리한 것이다.

| 계열 | 층 | 중앙 \|상대\| | CRV 절대 | CRV 상대 |
|---|---|---|---|---|
| ANs채널 | 전체 | 2.21 % | 0.96 | 0.97 |
| | < 0.1 px | 2.16 % | 1.11 | 1.05 |
| | ≥ 1 px | 3.48 % | 0.87 | 0.80 |
| PNs채널 | 전체 | 1.81 % | 0.85 | 1.03 |
| | < 0.1 px | 0.95 % | 0.93 | 1.09 |
| | ≥ 1 px | 5.46 % | 0.87 | 0.86 |
| ΣANs | 전체 | **11.25 %** | 0.99 | 1.01 |
| | < 0.1 px | 10.21 % | 1.10 | 0.99 |
| | ≥ 1 px | 18.58 % | 1.03 | 1.02 |

**CRV 가 안 준다** (0.80~1.09 대 0.83~1.11). 검색값으로 나눠도 아무것도 정리되지
않는다. **폐기 확정.**

따라서 §6.1 의 처방은 **"스캔별 섭동을 실제로 돌려야 한다"** 가 된다 — 후처리
Augur 에서는 가능하고, **Oculus 실시간에서는 불가능**하다. 그 제약을 명시한다.

분모 주의: ΣANs = (ANs채널 NO₂ − PNs채널 NO₂), 중앙 0.428 ppb, 0 이하가 31빈
(0.4 %). 상대값 계산에서 분모 ≤ 0.2 ppb 인 763빈(9.1 %)을 제외했다.

---

## 8. 최종값

> **ΣANs 구조항 3중 = 0.0758 ppb** (n=8382, 상자고착 제외, rSD, 빈 해상도)
> **SD 기준 0.1184 ppb** (꼬리 포함)
> **시간평균 0.0575 ppb**
> **짝지은 관측 게이트 0.0727 ppb 대비 0.79배**

두 채널 3중응답 상관 **r = +0.412**. 가법성 잔차 rSD 는 3중의 11.7 %(ANs) ·
44.7 %(PNs) · 27.5 %(ΣANs).

**문안**: 0.79배에 ±20 % 해상도 한계를 이미 인정했으므로 "게이트를 통과한다"가
아니라 **"게이트와 구분되지 않는다"** 가 정직하다. 여유를 통과로 파는 순간
심사자가 해상도 문단을 들고 온다.

---

## 9. cold 채널 (`[TBD-C]` 해소)

```
--key-a 3 --ef 0.164 --cache _cache_cold_op.npz --rt R_cold.npz
--out production_budget_cold.csv
```

운영 etalon f = **0.164** (실측 Δf cold +0.044 + 운영 고정 0.12; ANs 0.1594,
PNs 0.1368 과 같은 규약). 단일 채널이므로 ΣANs 차가 없다 — 출력의 ΣANs 행은
항등적으로 0 이고 무시한다. 채널 행만 유효하다. n=8629.

### 9-A. ⚠ cold 는 **다른 주(週)** 다 — 같은 표에 넣으면 안 된다

| 채널 | 구간 |
|---|---|
| hot ANs · PNs | **2026-05-18 ~ 05-24** |
| **cold** | **2026-06-10 ~ 06-16** |

같은 캠페인의 다른 주다. Table 2 에 cold 행을 넣으려면 **시간 기저가 다르다**고
명시해야 한다. 대기 조건·기기 상태가 다른 구간의 scale 을 같은 열에 세우면
"같은 이름, 다른 런, 다른 값" 을 표 안에서 재생산하는 것이다.

cold 구간은 알려진 **HK 선두 5열 결손 블록(06-11~06-15)** 과 거의 완전히 겹친다.
파서는 이를 처리한다(`core/raw_parser.py` `Cold6174HKMap`, ncols 6174 를
2026-yeosu 캠페인에 등록). 캐시의 T/P 를 직접 확인했다 — sentinel 0 없음, T
20.8~29.9 °C, P 1004~1008 hPa, 비물리값 0개. **다만 그 구간의 T/P 를 독립
출처와 대조한 적은 없다.**

### 9-B. shift 가 고정이다 — Δshift 열은 해당없음

cold 는 `Fix −0.5` (sq Fix 0.0). 실측으로 확인됐다:

```
shift 중앙 -0.500 px · 섭동이 shift 를 움직인 최대 0.0000 px
상자고착 0 / 8629 (0.0%) · 비수렴 0
```

**모든 빈에서 Δshift ≡ 0.** §4 의 Δshift 분해(분지 전환 검증)는 cold 에
적용되지 않는다. 상자고착이 0인 것도 구조적 귀결이지 안정성의 증거가 아니다.

### 9-C. §4.3 세 번째 사례 — 법칙이 다시 재현된다

| 항 | rSD (ppb) | SD (ppb) | s |
|---|---|---|---|
| i0 | 0.0988 | 0.4815 | **0.205** |
| rt | 0.0566 | 1.0591 | **0.053** |
| ef | 0.0174 | 0.0211 | 0.825 |
| **3중** | **0.1504** | **1.1891** | **0.127** |

| 공간 | 3중 | 제곱합 | 비 |
|---|---|---|---|
| rSD | 0.1504 | 0.1152 | **1.306** |
| SD | 1.1891 | 1.1636 | **1.022** |

**세 번째 재현이다.** SD 공간 비: knot 기저 0.946 · hot 빈 0.959~1.012 ·
**cold 빈 1.022**. 그동안 robust 공간 비의 범위는 0.983 ~ **1.306** 으로 벌어졌다.

분산 기여(SD 기준): **rt 83 % · i0 17 % · ef 0 %.**

cold 는 **robust 공간 기각의 최강 사례**다 — 31 % 과소평가. 다만 SD 공간
일치(1.022)는 rt 가 분산의 83 % 를 지배해 거의 자명하므로 **법칙의 강한
증거로 인용하면 안 된다**(§3-A). 법칙의 진짜 검증은 hot PNs 다.

### 9-D. 모양 통계 — cold 는 극단 사례다

| 항 | s | 분산 50 % | 분산 90 % | 연속구간 (무작위) |
|---|---|---|---|---|
| i0 | 0.205 | 1시간 (0.7 %) | 4시간 (2.6 %) | 1 (4) |
| **rt** | **0.053** | **2시간 (1.3 %)** | **5시간 (3.3 %)** | 2 (5) |
| ef | 0.825 | 17시간 (11.1 %) | 100시간 (65.4 %) | 34 (35) |
| tri | 0.127 | 2시간 (1.3 %) | 5시간 (3.3 %) | 3 (5) |

§6.1a 가 hot 보다 훨씬 선명하게 성립한다. **5시간(3.3 %)이 rt 분산의 90 %** 를
지고, ef 는 100시간(65 %)에 퍼져 있으며 연속구간 수가 무작위와 같다(34 vs 35).

최악 구간: **06-16 UTC 20~21시 두 시간이 3중 분산의 53.1 %**, 06-15 UTC 12시가
24.7 %.

알려진 `05-21 09~11 KST cold 계단` 은 이 창(06-10~06-16)에 없다 — 이 결과와
무관하다.

### 9-E. cold 는 §8 최종값에 들어가지 않는다

시간 기저가 다르고(9-A), Δshift 검증이 불가능하며(9-B), 관측 게이트와 짝지을
ΣPNs 산출물이 없다. **§4.3 의 사례**와 **§6.1a 의 극단 사례**로만 쓴다.

---

## 10. 1/4 블록 N-스케일링 (`[TBD-B2]`)

ZA 블록(34 스캔, span 32 s)을 잘라 **블록평균 I₀ 잡음이 스캔 수로 어떻게 주는가**
를 직접 잰다. §6.2 의 "블록 ×4 → i0 항 ×1/2" 가 이 지수에 통째로 기댄다.

정의 — 핏 없이 I₀ 공간에서(N 스케일링은 스펙트럼 잡음의 성질이지 핏의 성질이
아니다). 부분블록 A, B 의 차를 핏창 안에서 상대 RMS 로:

```
d(A,B) = rms_pix( (A − B) / full )
```

* **짝/홀 반블록**(각 17 스캔): 같은 시간분포 → 드리프트 상쇄 → 순수 잡음
* **연속 4분할**(각 8 스캔): 시간순 절단 → 블록 내 구조 잔존. 지레팔 L = 1,2,3

백색이면 (a) d 가 L 에 무관하고 (b) `d_q/d_h = √(17/8) = 1.458` 이다.
**스캔 수 비는 2 가 아니라 2.125 다**(34 → 17 / 8) — log2 로 지수를 내면 틀린다.

**기저를 섞으면 안 된다.** `d_q`(연속 4분할)는 드리프트가 남아 있고
`d_h`(짝/홀)는 상쇄돼 있다. 둘을 나누면 스케일링과 시간구조가 섞인 수가 나온다.
그래서 두 질문을 갈라 잰다.

* **① 스케일링** — 연속 대 연속: 4분할(8스캔) 대 연속 반블록 `(q0+q1)/2 − (q2+q3)/2`(16스캔)
* **② 시간구조** — 같은 m 에서: 연속 반블록(16) 대 짝/홀 반블록(17)

| | hot ANs | hot PNs |
|---|---|---|
| 연속 4분할 (8스캔) | 2.5064e-04 | 1.0947e-04 |
| 연속 반블록 (16스캔) | 2.3320e-04 | 8.2041e-05 |
| 짝/홀 반블록 (17스캔) | 1.0335e-04 | 6.7426e-05 |
| **① 지수 p** (백색 0.500) | **0.104** | **0.416** |
| **② 연속/짝홀** (구조없음 1.031) | **2.256** | **1.217** |

### 10-A. ① — ANs 는 스캔을 늘려도 잡음이 거의 안 준다

**p = 0.104 (ANs) · 0.416 (PNs).** PNs 는 백색에 가깝지만 **ANs 는 사실상 안
준다** — 스캔을 2배로 해도 7 % 감소다(백색이면 29 %).

⚠ 앞선 판본의 `p = 1.175` 는 **기저 혼합이 만든 허수**였다(연속 d_q ÷ 짝/홀 d_h).
0.5 를 넘는 지수는 물리적으로 나올 수 없고, 그게 경보였어야 했다.

### 10-B. ② — 블록 안에 시간순 구조가 있다

같은 스캔 수인데 **연속으로 자르면 짝/홀보다 2.26배(ANs) · 1.22배(PNs)** 크다.
시간 순서에만 의존하는 구조가 있다는 뜻이다.

두 채널 모두 유의한 단조 추세가 보인다(부호있는 편차, 전 블록 중앙):

| | q0 | q1 | q2 | q3 | 표준오차 |
|---|---|---|---|---|---|
| hot ANs | **+248** | −81 | −211 | **−314** ppm | 54~99 |
| hot PNs | **−261** | −34 | +78 | **+235** ppm | 16~21 |

**부호가 반대다** — 32 초 동안 ANs 창은 내려가고 PNs 창은 올라간다. 인접한 두
창(429.5–461.9 / 444.1–470.6 nm)이 반대로 움직이려면 회전축이 있어야 하므로
**강도 변화가 아니라 스펙트럼 기울기 변화**다. 가스 충전으로는 설명되지 않고
(둘 다 같은 부호여야 한다), 광원이나 **채널을 가르는 밴드패스** 쪽이다
(6/5 필드로그의 ROI2 밴드패스 교체 기록도 같은 방향).

다만 선형추세를 죽이는 대비 `c = (q0−q1−q2+q3)/2` 로 재도 초과가 줄지 않으므로
(ANs +88.4 %, PNs +16.8 %) **선형 드리프트가 주범은 아니다** — 본체는 스캔간
상관 잡음이다. 관찰로 기록하고 여기서 멈춘다(AMT 범위 밖).

**기존 가드로는 안 보인다.** worker Pass 1 에는 블록 선두 과도구간 트리머가
이미 있다(`gui/worker.py:2444` `settle_start`). 그런데 문턱이 `SETTLE_TOL = 2 %`
이고 — 인젝션 자료의 14~21 % 과도구간을 겨냥해 만든 값이다 — 여기서 잰 블록내
변동은 **0.025 %** 라 두 자릿수 아래다. 여수 ZA 블록에서는 `k = 0`, 즉 한 행도
안 잘린다. 버그가 아니라 **설계 범위 밖**이다. 이 진단의 `_block_average` 가
트리머를 빼고 복사한 것도 같은 이유로 무해하다(그쪽 독스트링에 명시).

### 10-C. §6.2 에 무엇을 쓸 수 있나

**채널마다 다르다: p = 0.104 대 0.416.** 단일값을 쓸 수 없다.

그리고 이 설계는 `n < N` 구간만 본다 — 블록을 **쪼개는** 방법으로는 블록을
**늘렸을 때**를 알 수 없다. `N > 34` 로의 외삽은 어느 방향으로도 검증되지 않는다.

> §6.2 의 "블록 ×4 → i0 항 ×1/2" 는 이 측정으로 **확인되지 않았다.** 다만
> 잴 수 있는 구간(8→16 스캔)에서 ANs 의 p = 0.104 는 **가정과 어긋나는
> 방향**이다. 확인하려면 긴 ZA 블록을 실제로 한 번 운용해야 한다 — `[TBD-E]`.

기존 "초과 5.4 %" 는 NO₂ 응답 공간 값이라 이 I₀ 공간 값과 직접 비교할 수 없다.

---

## 11. He 듀티사이클 (`[TBD-A2]` 해소)

계기 제원이 아니라 **운용 기록**이라 데이터가 답을 갖고 있었다. 상태 플래그
규약은 `core/raw_parser.py:21` — `1=Atmosphere, 500=ZA, 510=He, 502/512=wait,
503/513=end`. `measure_i0_loo.collect()` 에 He 분기가 이미 있었으나 `pass` 였다
(식별해놓고 버리고 있었다). `_time_blocks()` 로 블록 시각·스캔수·span 만 받아
캐시에 넣었다 — §6.2 는 N·span·주기만 쓰므로 스펙트럼은 안 받는다.

관측 구간 155.8 h (2026-05-18 ~ 05-24):

| 블록 | 개수 | 스캔/블록 | span [s] | 주기 [h] |
|---|---|---|---|---|
| ZA | 157 | 34 | 32.0 | **1.00** |
| He | 54 | 34 | 32.0 | **3.00** |

**ZA 매시, He 3시간마다, 둘 다 34 스캔 32 초.**

| | 시간 | 비율 |
|---|---|---|
| ZA 총 | 1.42 h | 0.91 % |
| He 총 | 0.49 h | 0.31 % |
| **교정 합계** | **1.90 h** | **듀티 1.2 %** |

측정 손실이 1.2 % 뿐이다 — **어떤 처방도 듀티사이클 때문에 막히지 않는다.**
이게 §6 전체의 배경 논거다.

### 11-A. He 주기가 커버리지 비대칭의 원인이다

두 블록은 **제원이 완전히 같다**(34 스캔, 32 s). 다른 건 주기뿐이다.
그런데 knot 을 하나 놓쳤을 때 생기는 구멍은 주기에 비례한다:

| | 주기 | 결손 1회당 구멍 | 듀티 |
|---|---|---|---|
| ZA | 1.00 h | 2 h | 0.90 % |
| He | **3.00 h** | **6 h** | 0.31 % |

앞서 잰 **참조 커버리지 결손(ZA 0.0 % · R 4.1~5.0 %)** 이 이걸로 설명된다.
측정 품질의 차이가 아니라 **스케줄의 비대칭**이다.

> **He 주기를 ZA 와 맞추면 총 교정 듀티 1.20 % → 1.79 %**, 그 대가로 미기술
> 구간 4~5 % 의 대부분이 사라진다.

근거는 **정밀도가 아니라 커버리지**로 세워야 한다. R 오차는 간격에 거의
무관하므로(지수 0.13~0.26) 더 자주 재도 **잘 측정된 스캔이 더 정확해지지는
않는다** — 측정 안 된 스캔이 덜 생길 뿐이다.

### 11-B. 블록을 늘리는 쪽은 별개다

ZA 블록을 ×4(128 s)로 하면 교정 합계 6.07 h → 듀티 3.9 %. 여유는 있으나
§10-C 때문에 **이득의 크기는 예측 불가**다. 주기 처방(11-A)과 블록 처방은
근거가 다르다 — 전자는 커버리지, 후자는 정밀도.

---

## 12. 남은 것

| | 항목 | 비고 |
|---|---|---|
| — | **긴 ZA 블록 1회 운용** | §10-B — `N>34` 스케일링은 이 방법으로 못 잰다 |
| — | 블록 내 32 s 추세의 정체 | §10-B — 두 채널 부호 반대, 광원/밴드패스 기울기 의심 |
| — | cold 06-11~06-15 T/P 독립 대조 | 파서는 처리하나 외부 검증 없음 |

해소됨: `[TBD-A2]` → §11 · hot ANs 1/4 블록 → §10 · `[TBD-C]` cold → §9.
`[TBD-3]`(백분율 분모)은 **폐기** — Table 2 를 ppb 로만 싣고 보고 불확도 대비
배수를 본문에 두면 분모가 하나 준다.

---

## 부록: 도구

`diagnostics/i0_interp_2026-09/production_budget.py`

병렬은 **knot 단위**다 — LOO 보간자와 격자 시딩이 knot 당 한 번이고 그 knot 의
모든 빈이 공유하므로, 빈 단위로 흩으면 워커마다 시딩을 다시 해서 60배 손해다.
knot 은 라운드로빈으로 배분한다(knot 마다 빈 수가 달라 앞뒤로 자르면 워커 하나가
꼬리를 뒤집어쓴다).

**워커 수가 결과를 바꾸지 않는다** — 2026-09-21 확인:

| 확인 | 결과 |
|---|---|
| `--jobs 1` vs `--jobs 4` (limit 300) | CSV 바이트 동일 |
| 병렬화 이전 직렬 코드 vs `--jobs 1` | CSV 바이트 동일 |
| 직렬 전 빈(67분) vs 20워커 전 빈(8분) | 출력 숫자 전부 동일 |

데이터가 필요해 CI 에는 못 넣는다. 병렬부를 고치면 위 diff 를 직접 돌려라.

워커마다 npz 를 따로 들므로(채널당 ~350 MB) 20워커면 ~9 GB 를 쓴다. 모자라면
`--jobs` 로 줄인다. 워커의 BLAS 스레드는 spawn 전에 1로 박는다 — 각자 띄우면
코어를 서로 뺏는다.
