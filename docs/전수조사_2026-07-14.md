# CAESAR Pro 전수조사 리포트 (2026-07-14)

교수님 미팅 준비를 위한 전 모듈 감사. 코어(physics/doas_fit/engine/data_io/raw_parser/
result_io) + 워커(AnalysisWorker/AlphaExportWorker 전체) + 메인창(좌패널·RUN·QC·저장·
Setup 탭) + Alpha Generator + reflectance_calc 정독, 나머지 모듈(Result Lab·Plot Maker·
R Calibrator·Monitor)은 구조 확인 + 기존 검증 기록 + 자동 검증기 실행으로 확인.

## 검증기 실행 결과 (이 세션에서 실측)

- `validate_pipeline.py`: **4 PASS · 0 FAIL · 2 SKIP** — SKIP 2건은 외부 데이터 파일
  부재(raw 대조 파일, 핏 출력 폴더)로 이 머신에서 실행 불가일 뿐 코드 문제 아님.
- `validate_plotmaker.py`: **24/24 PASS**.

## 수정 완료 (이 세션)

| 항목 | 내용 |
|---|---|
| 툴팁-기본값 불일치 | `chk_allow_neg` 툴팁 "Default = off" → 실제 기본 ON에 맞게 수정. `chk_qc` 툴팁 "Default = on" → 실제 기본 OFF(플래그 우선 철학)에 맞게 수정 |
| README 드리프트 | "Result Viewer"→"Result Lab", 타임스탬프 mtime 설명→bytepack 실사용 반영, `ui_plot_maker.py`→패키지 표기 |

## 발견했지만 수정하지 않은 것 (잠재/판단 필요)

1. **Temporal I₀ 인덱스 불일치 (레거시, 도달 불가)** — `worker._prescan_za_scans`는
   file_list 인덱스로 ZA를 수집하는데 `_interpolate_i0(za_list, i)`는 확장 스캔
   인덱스로 호출됨. 현재는 raw 직접 핏이 차단(알파 전용)이라 실행 불가 경로.
   → raw 핏 부활 시 반드시 수정. 그전엔 dead code 정리 후보.
2. **doas_fit custom_basis 시 poly_coeffs 오염 (잠재)** — `poly_coeffs =
   c_opt[num_gases:-2]`가 custom_basis 계수까지 포함. 현재 custom_basis=None이라
   무해하나 fixed-pattern 베이스 연결 시 반환 분리 필요.
3. **다채널 저장 공통 헤더** — save()의 # 공통 헤더(poly/λ/robust)와 채널 헤더의
   `refs=`가 활성 채널 값 기준. 채널별 태그 줄이 따로 있어 실害 낮지만 헤더만 보면
   오해 여지.
4. **QC K 기본값 8 vs 캠페인 운용값 6** — 의도된 보수적(과제거 방지) 기본인지 확인
   필요. 시나리오 JSON이 K를 갖고 다니면 무해.
5. **n_air 상수** — worker 2.68678e19 vs physics 2.6867811e19 (0.4 ppm, 무시 가능
   하지만 한 곳으로 통일하면 깔끔).
6. **Settling의 '빈' 정의 의존성** — 정착 스캔 제외는 "α 파일 = raw 1시간 파일 1개"
   구조에 의존(파일 첫 N행 = 주입 직후). α를 일 단위로 병합 생성하도록 바꾸면
   조용히 의미가 깨짐 — α 생성 단위를 바꿀 때 함께 볼 것.
7. **릴리즈 체크리스트와 SKIP** — CHANGELOG 체크리스트는 "6항목 전부 PASS"인데
   외부 데이터 없는 머신에선 4개만 실행됨. 체크리스트에 "SKIP 없는 환경에서" 명시
   필요.

## 논리 점검 결과 — 임의 결정으로 보였으나 근거가 확인된 것들

- Etalon 주파수 1회 검출 후 전체 공유: 순차 경로와 병렬 경로 동일 정책, fringe
  주파수는 광학 구조 기인이라 일중 안정 가정이 합리적.
- contrast band 0.05–0.35, R(t) knot 이상치 [0.5×, 2×] Leff 밴드, 블록 분할
  gap=10, 청크 워밍업 40: 모두 코드 주석에 실측 근거가 기록되어 있고 병렬=순차
  검증으로 뒷받침됨.
- R_L이 괄호 전체에 곱해지는 위치, Fix 모드 절대값 처리, R clip 전 판정: 과거
  버그를 고친 이력이 주석·메모리에 남아 있고 현재 구현이 옳음.
- 비파괴 QC·±Neg 기본 ON·raw 컬럼 단일출처: 데이터 무결성 헌장과 일관.

## 개선 제안 (우선순위)

### 단기 (다음 캠페인 전)
1. **커밋/태깅 마무리** — 미커밋 변경 정리 후 v0.1.x 태깅 (재현성 체계가 git 해시
   기반인데 미커밋 상태면 provenance가 'dirty'로 남음).
2. **콜드 잔차 고정패턴 제거** — H₂O/O₄ 레퍼런스 보강 + 이미 구현된 custom_basis에
   고정패턴 고유벡터 연결(위 잠재이슈 2 수정과 함께). NO₂ 불확도 개선의 최대 헤드룸.
3. **핫 2채널 원클릭** — R(t) 픽커 멀티선택 + 채널 자동 매칭(사용자 기 요청,
   pending-work).

### 중기
4. **계통오차 예산 리포트** — σ(R), σ(d), 단면 문헌 불확도, R_L 불확도를 합성한
   per-species 오차 예산을 결과 헤더에 자동 기록. 논문 Table 재료가 됨.
5. **R 계산 속도** — 2파스→1파스 + 핫 채널 스캔 공유 (~50→15분 예상, pending-work).
6. **알파/파싱 캐시 (E-1/E-2)** — 같은 raw 재처리 시 파싱 결과 재사용.
7. **레거시 정리** — raw 직접 핏 경로 + Temporal I₀ 제거 여부 결정(보존한다면
   인덱스 버그 수정).

### 장기
8. **ANs real-vs-thermal 분해 및 스케일 보정** — 얼라인먼트-스케일 상관(r=+0.76)
   후속: 기간별 b 보정 적용 여부를 박사님과 결정.
9. **배포 패키징** — requirements-lock 기반 재현 환경, Python 최소버전 명시,
   HITRAN 오프라인 번들.
10. **사용자 매뉴얼** — README(개발자용)와 별도로 조작 순서 중심 매뉴얼.
